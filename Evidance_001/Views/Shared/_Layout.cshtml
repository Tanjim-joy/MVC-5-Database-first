<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Md.Tangimul Haque</title>

    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.bundle.js"></script>
    <style>
        body {
            padding-top: 70px;
        }
    </style>
    @RenderSection("styles", false)
</head>
<body>
    <nav class="navbar navbar-dark bg-success navbar-expand-md fixed-top border-bottom mb-1">
        <div class="container-fluid">
            <span class="navbar-brand ms-5">Evidance Exam</span>
            <div class="navbar-collapse collapse">
                <ul class="navbar-nav m-lg-auto">
                    <li class="nav-item">
                        @Html.ActionLink("Home", "Index", "Home", new { }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Vechicle", "Index", "Vehicles", new { }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Car", "Index", "Car", new { }, new { @class = "nav-link" })
                    </li>

                </ul>
            </div>
        </div>
    </nav>
    <div class="container-fluid" style="min-height:calc(100vh - 140px)">
        @RenderBody()
    </div>
    <footer class="container">
        &copy;@DateTime.Now.Year - MVC5 Application
    </footer>
    @RenderSection("scripts", false)
</body>
</html>